# Applications

Each app is independently deployable. Shared product logic belongs in `packages/`.

- `web` — marketing site and public creator storefronts
- `dashboard` — authenticated creator workspace
- `api` — APIs, webhooks, integrations, and background-job entry points
