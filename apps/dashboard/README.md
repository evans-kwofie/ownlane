# Ownlane Dashboard

React Router Framework Mode application for the authenticated creator workspace.

## Commands

```bash
pnpm --filter dashboard dev
pnpm --filter dashboard build
pnpm --filter dashboard typecheck
```

The dashboard starts in SPA mode and communicates with the API application. Shared UI and domain logic belong in `packages/`.
